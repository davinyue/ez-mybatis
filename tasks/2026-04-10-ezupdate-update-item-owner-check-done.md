# 任务背景

用户希望分析 `EzUpdate` 是否可以判断更新项归属，并要求校验更新项必须属于当前更新语句允许更新的表。

# 需求

- 为 `EzUpdate` 增加更新项归属校验
- 校验规则按同一个 `Table` 对象实例判断
- 允许更新主表实例和 join 表实例
- 做最小必要验证

# 任务列表

- [x] 分析 `EzUpdate`、`UpdateSetBuilder`、`UpdateItem` 的结构
- [x] 设计归属校验落点
- [x] 实现构建期校验
- [x] 补充最小单元测试
- [x] 完成验证

# 完成进度

- 已在 `EzUpdate.build()` 增加统一校验，仅允许主表实例和 join 树中的表实例
- 已补充 core 层单元测试，覆盖同实例通过、join 表通过、无关实例失败三种情况
